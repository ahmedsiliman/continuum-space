import LayoutCenter from '../layouts/LayoutCenter';
import LayoutSplit from '../layouts/LayoutSplit';
import LayoutCv from '../layouts/LayoutCv';
import BIMDashboard from '../components/BIMDashboard';

export const layoutRegistry = {
  center: LayoutCenter,
  split: LayoutSplit,
  cv: LayoutCv,
  bim_dashboard: BIMDashboard
};
